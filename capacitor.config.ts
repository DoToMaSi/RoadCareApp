import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roadcareapp.rockettsally',
  appName: 'RoadCareApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
